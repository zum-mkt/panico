export type Partner = {
  id: string;
  name: string;
  logo_url: string | null;
  photo_url: string | null;
  description: string | null;
  link_url: string | null;
  position: number;
  is_active: boolean;
};
